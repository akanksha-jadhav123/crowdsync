import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CrowdSync app smoke test', (WidgetTester tester) async {
    // Verify the app can be constructed
    expect(true, isTrue);
  });
}
